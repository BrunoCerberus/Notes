# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

inhibit_all_warnings!

target 'Notes' do
  use_frameworks!
  pod 'SwiftGRPC', :git => 'https://github.com/grpc/grpc-swift', :branch => 'cgrpc', :inhibit_warnings => true

end
