# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Notes' do
  use_frameworks!
  pod 'SwiftGRPC', :git => 'https://github.com/grpc/grpc-swift', :branch => 'cgrpc'

end
